-- Enable syntax highlighting
vim.opt.syntax = "on"

-- Set colourscheme safely
-- local ok, _ = pcall(vim.cmd, "colorscheme dracula")
-- if not ok then
--   vim.notify("Theme 'dracula' not found!", vim.log.levels.WARN)
-- end

